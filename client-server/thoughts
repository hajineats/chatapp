Server starts.
Client connects. Receives a user id generated by a server (port number?)
Client selects a port to communicate with.
Client sends a message in the form of: [isIndividualChat, sender port, receiver port, message]
Server receives this message and sends:

User:
    nickname
    port_number
    unique_id (key, assigned from 0~999)

Server:
    set<User> clients
    max_id

when client comes in,
    max_id++
    clients.add(new User(nick, port, max_id))

when client sends a message




